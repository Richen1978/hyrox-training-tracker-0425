import React from 'react';
import ReactDOM from 'react-dom/client';
import HyroxTrainingTracker from './tracker';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <HyroxTrainingTracker />
  </React.StrictMode>
);